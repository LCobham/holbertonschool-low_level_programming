#include <stdio.h>
/**
 * main - Prints a specific string
 *
 * Return: 0 if no error is present
 */

int main(void)
{
	const char *s = "\"Programming is like building a multilingual puzzle";

	puts(s);
	return (0);
}
