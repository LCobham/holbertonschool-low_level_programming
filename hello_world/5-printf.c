#include <stdio.h>
/*
 * main - print specific string with printf
 *
 * Return: 0 if no error is present
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
