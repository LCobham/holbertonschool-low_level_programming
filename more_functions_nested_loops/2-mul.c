#include "main.h"

/**
 * mul - returns the multiplication of both arguments
 * @a: int which whill be multiplied
 * @b: int which will be multiplied
 *
 * Return: the result of the multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
