#include <stdio.h>

/**
 * main - prints the name of the executable
 *
 * Return: 0 on success
 */

int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
